

# Data types:

# numbers:
var num = (15.25 + -5) * 3;

# strings:
var my_string = "samir script !";

# booleans:
var my_bool = true;
var my_other_bool = not false;
var my_other_other_bool = ! (5 < 4); # '!' and 'not' are both valid.

# Statements:

print num; print my_string;
print my_bool; print my_other_other_bool;