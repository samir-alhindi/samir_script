

func list_sum(list){
    var sum = 0;
    var index = 0;
    while index < list.size do {
        sum = sum + list.get(index);
        index = index + 1;
    }
    return sum;
}

var numbers = List();
numbers.add(10);
numbers.add(15);
numbers.add(3);


println list_sum(numbers);