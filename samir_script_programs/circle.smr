

class Circle(radius: number){
    var diameter = radius * 2;
    func getArea(){
        return PI * (radius * radius);
    }
}

var my_circle = Circle(5);
println my_circle.getArea();

var other_circle = Circle(10);
println other_circle.getArea();