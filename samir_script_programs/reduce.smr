

var numbers = [1, 2, 3, 4, 5];
var chars = ["H", "E", "L", "L", "O"];
println numbers.reduce(lambda x, y -> x + y);
println numbers.reduce(lambda x, y -> x * y);
println chars.reduce(lambda a, b -> a + b);

var max = numbers.reduce(lambda x, y -> x if x > y else y);
println "Max: " + str(max);

var nested_list = [[1, 2], [3, 4], [5, 6]];
var flattened = nested_list.reduce(lambda x, y -> x + y);
println "Flattened: " + str(flattened);